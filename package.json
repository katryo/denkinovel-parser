{
  "name": "denkinovel-parser",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "create": "npm run build && npm run test",
    "build": "tsc -p .",
    "test": "vitest",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install",
    "preinstall": "npx only-allow pnpm"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/node": "16.11.36",
    "nodemon": "3.1.7",
    "ts-node": "10.7.0",
    "typescript": "4.6.4",
    "vitest": "0.12.6"
  }
}
